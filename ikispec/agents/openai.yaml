interface:
  display_name: "ikispec"
  short_description: "The project/ spec output shapes, authority boundaries, and hard invariants."
  default_prompt: "$ikispec"
policy:
  allow_implicit_invocation: false
